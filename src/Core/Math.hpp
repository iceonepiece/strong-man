#pragma once

struct vec2
{
  float x;
  float y;

  vec2() {}
  vec2(float x_, float y_) : x(x_), y(y_) {}
};

struct vec3
{
  float x;
  float y;
  float z;

  vec3() {}
  vec3(float x_, float y_, float z_) : x(x_), y(y_), z(z_) {}
};
